# Used when running: npx cap sync ios
# Point at your deployed BrainDump URL (same as users get in the browser).
CAPACITOR_SERVER_URL=https://your-production-domain.com

# Optional: also read by capacitor.config.ts if set in the shell
NEXT_PUBLIC_APP_URL=https://your-production-domain.com
