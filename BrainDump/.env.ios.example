# Used when running: npx cap sync ios
# Default in capacitor.config.ts is production; override for staging or local dev.
# CAPACITOR_SERVER_URL=https://saas-silk-tau.vercel.app

# Optional: also read by capacitor.config.ts (e.g. Saas/.env.local)
NEXT_PUBLIC_APP_URL=https://saas-silk-tau.vercel.app
